float:
  any:
  - base_args: ['@metric']
    constructor: PGVectorHNSWBQUnpartitioned
    disabled: false
    docker_tag: ann-benchmarks-pgvector-hnsw-bq-unpartitioned
    module: ann_benchmarks.algorithms.pgvector_hnsw_bq_unpartitioned
    name: pgvector_hnsw_bq_unpartitioned
    run_groups:
      pgvector_hnsw_bq_unpartitioned1:
        args: [
            ["postgres://ann:ann@localhost:5432/ann"],
            [16], # m
            [64], # ef_construction
          ]
        query_args: [[500, 600, 700, 800, 900, 1000, 1200, 1400]] # ef_search
